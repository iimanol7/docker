FROM nginx:alpine

COPY ./sitio-web/ /usr/share/nginx/html

RUN echo "🎉 Construyendo imagen Nginx con saludo personalizado"
# Este mensaje no se ve en el navegador, solo aparece al hacer docker build